apiVersion: v1
kind: Service
metadata:
  name: llm-model
  namespace: model-serving
  labels:
    app: llm-model
    component: model-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: llm-model

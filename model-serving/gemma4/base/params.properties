# Default vLLM runtime knobs (consumed by the gemma4-params ConfigMap).
# These are the DEFAULTS baked into the base so `oc apply -k base` works
# standalone. Per-run overrides live in scripts/config.conf and are applied by
# the generated overlay (overlays/current).
MODEL_ID=google/gemma-4-31B-it
SERVED_NAME=gemma-4
TP_SIZE=2
MAX_MODEL_LEN=8192
GPU_MEM_UTIL=0.90

apiVersion: template.openshift.io/v1
kind: Template
metadata:
  name: open-webui
parameters:
  - name: MODEL_URL
    description: Base URL of the OpenAI-compatible model endpoint (e.g. https://granite-model-mm-test.apps.ocp-test.nerc.mghpcc.org)
    required: true
objects:
  - apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: open-webui-pvc
    spec:
      accessModes:
        - ReadWriteOnce
      volumeMode: Filesystem
      resources:
        requests:
          storage: 2Gi

  - apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: open-webui
    spec:
      replicas: 1
      strategy:
        type: Recreate
      selector:
        matchLabels:
          app: open-webui
      template:
        metadata:
          labels:
            app: open-webui
        spec:
          containers:
            - name: open-webui
              image: ghcr.io/open-webui/open-webui:main
              ports:
                - containerPort: 8080
                  protocol: TCP
              env:
                - name: OPENAI_API_BASE_URL
                  value: "${MODEL_URL}/v1"
                - name: OPENAI_API_KEY
                  value: "unused"
                - name: WEBUI_AUTH
                  value: "False"
                - name: ENABLE_OLLAMA_API
                  value: "False"
              volumeMounts:
                - name: data
                  mountPath: /app/backend/data
              resources:
                requests:
                  cpu: 100m
                  memory: 512Mi
                limits:
                  cpu: "1"
                  memory: 2Gi
              livenessProbe:
                httpGet:
                  path: /health
                  port: 8080
                initialDelaySeconds: 30
                periodSeconds: 10
              readinessProbe:
                httpGet:
                  path: /health
                  port: 8080
                initialDelaySeconds: 15
                periodSeconds: 5
          volumes:
            - name: data
              persistentVolumeClaim:
                claimName: open-webui-pvc

  - apiVersion: v1
    kind: Service
    metadata:
      name: open-webui-service
    spec:
      type: ClusterIP
      selector:
        app: open-webui
      ports:
        - name: ui
          port: 8080
          targetPort: 8080
          protocol: TCP

  - apiVersion: route.openshift.io/v1
    kind: Route
    metadata:
      name: open-webui
    spec:
      to:
        kind: Service
        name: open-webui-service
      port:
        targetPort: ui
      tls:
        termination: edge
        insecureEdgeTerminationPolicy: Redirect

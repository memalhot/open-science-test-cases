# Assisted by Claude Opus 4.6
apiVersion: v1
kind: Pod
metadata:
  name: t8-chunked-prefill-moc-r4pcc02u05-pass-fail
  namespace: uat-project
  labels:
    app.kubernetes.io/managed-by: uat-generator
    test: chunked-prefill
    dag-step: pass-fail
    node: moc-r4pcc02u05
    sweep: pass-fail
spec:
  tolerations:
    - key: nvidia.com/gpu.product
      operator: Exists
      effect: NoSchedule
  nodeSelector:
    kubernetes.io/hostname: moc-r4pcc02u05
  containers:
    - name: t8-chunked-prefill-moc-r4pcc02u05-pass-fail
      image: registry.redhat.io/ubi9/ubi:9.8
      command:
        - /binaries/chunked-prefill/test.bin
        - --ginkgo.label-filter=pass-fail
        - --ginkgo.junit-report=/uat_workspace/junit.xml
      env:
        - name: SERVER_URL
          value: "http://svc-t8-chunked-prefill-moc-r4pcc02u05-vllm-server:8000"
        - name: MODEL_NAME
          value: /models/ibm-granite/granite-3.3-8b-instruct
        - name: RESULTS_DIR
          value: /uat_workspace
      volumeMounts:
        - name: workspace
          mountPath: /uat_workspace
          subPath: uat/results/manual-run/t8-chunked-prefill-moc-r4pcc02u05-pass-fail
        - name: workspace
          mountPath: /binaries
          subPath: uat/results/manual-run/binaries
        - name: models
          mountPath: /models
          readOnly: true
  volumes:
    - name: workspace
      persistentVolumeClaim:
        claimName: uat-project-storage
    - name: models
      persistentVolumeClaim:
        claimName: uat-models
  restartPolicy: Never

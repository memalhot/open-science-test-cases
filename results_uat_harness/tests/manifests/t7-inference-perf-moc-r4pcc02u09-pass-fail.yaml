# Assisted by Claude Opus 4.6
apiVersion: v1
kind: Pod
metadata:
  name: t7-inference-perf-moc-r4pcc02u09-pass-fail
  namespace: uat-project
  labels:
    app.kubernetes.io/managed-by: uat-generator
    test: inference-perf
    dag-step: pass-fail
    node: moc-r4pcc02u09
    sweep: pass-fail
spec:
  tolerations:
    - key: nvidia.com/gpu.product
      operator: Exists
      effect: NoSchedule
  nodeSelector:
    kubernetes.io/hostname: moc-r4pcc02u09
  containers:
    - name: t7-inference-perf-moc-r4pcc02u09-pass-fail
      image: quay.io/inference-perf/inference-perf:v0.6.1
      command:
        - /binaries/inference-perf/test.bin
        - --ginkgo.label-filter=pass-fail
        - --ginkgo.junit-report=/uat_workspace/junit.xml
      env:
        - name: HOME
          value: /tmp
        - name: SERVER_URL
          value: "http://svc-t7-inference-perf-moc-r4pcc02u09-vllm-server:8000"
        - name: RESULTS_DIR
          value: /uat_workspace
      volumeMounts:
        - name: workspace
          mountPath: /uat_workspace
          subPath: uat/results/manual-run/t7-inference-perf-moc-r4pcc02u09-pass-fail
        - name: workspace
          mountPath: /binaries
          subPath: uat/results/manual-run/binaries
        - name: models
          mountPath: /models
          readOnly: true
  volumes:
    - name: workspace
      persistentVolumeClaim:
        claimName: uat-project-storage
    - name: models
      persistentVolumeClaim:
        claimName: uat-models
  restartPolicy: Never
